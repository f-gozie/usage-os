/** The privacy footer. Plain, honest, no slogan (copy-voice). Inverted bar. */
export function Footer() {
  return (
    <div className="bg-bar-bg px-3 py-[11px] text-center text-[10px] font-semibold uppercase tracking-[0.22em] text-bar-fg">
      Your data never leaves this machine
    </div>
  );
}
