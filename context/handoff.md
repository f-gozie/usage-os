# Handoffs moved

Handoffs are no longer a single overwritten file — they're an append-only journal
**per plan**, so the full session-by-session history is preserved.

➡ **Start at the registry: [`context/plans/README.md`](plans/README.md)**, pick the
`active` plan, and read the **newest** file in its `handoffs/` folder.

The current state lives at
[`context/plans/2026-06-22-product-redesign/handoffs/`](plans/2026-06-22-product-redesign/handoffs/)
(newest = latest). This stub is a static breadcrumb — it never needs updating.
