# Handoff — 2026-09-13-01 · PR #41 merged; v0.1.2 released

## 1. How this session started

A status check after six weeks away. Findings: the last push was 31 Jul; the v0.1.2 release had
been sitting as a **draft with no DMG** since that day, while the landing page already linked to
`UsageOS-0.1.2-universal.dmg` — so the site's download button had returned 404 for the whole
gap (Homebrew kept working because the cask still pointed at 0.1.1). Traction at the time of
the check: 33 DMG downloads across 0.1.0 + 0.1.1, 6 stars (two organic, 28–29 Jul), 0 issues,
10 unique repo visitors in 14 days, cask below Homebrew's analytics floor.

## 2. What shipped

- **PR #41 merged** (capture-permission health, D71) via the REST merge endpoint; branch deleted.
- **CHANGELOG** gained the permission line for 0.1.2 (`5accb12`).
- **v0.1.2 released**: tag `v0.1.2` → `5accb12`. Built with `scripts/release-macos.sh`
  (first run failed: `node_modules` was missing → `npm ci` first). Tauri notarized the app but
  **not the DMG** — same gap as v0.1.0/0.1.1 — so the DMG was submitted to notarytool
  separately, stapled, and verified Gatekeeper-clean on a quarantined copy.
  Assets: `UsageOS-0.1.2-universal.dmg`, `UsageOS.app.tar.gz`, `latest.json`.
  DMG sha256 `abec6b32452fc5604924dd568be9807df841c60d7016ffe3f75501c370bcdd10`.
- Release notes rewritten: the 31 Jul "known issue" paragraph became the fix bullet, plus a
  note for 0.1.1 users to re-check permissions.
- **Cask bumped** to 0.1.2 (`f-gozie/homebrew-tap` `b630c42`).
- Landing untouched — its link already named the 0.1.2 DMG and now resolves.

## 3. Owner next steps

1. **Update this machine** (still degraded per the 25 Jul audit): updater from 0.1.1, or
   `brew upgrade --cask f-gozie/tap/usageos`. Migration `0008` re-sorts local history on first
   launch; the tray's "Fix recording permissions…" item should appear within a minute. Re-grant
   Accessibility (switch off and on) and Automation, watch it clear. That is the open on-device
   verification item on plan item 4.
2. **PR #42** (landing PostHog analytics) is still open and green. Merging needs a PostHog
   project + `PUBLIC_POSTHOG_KEY` in Cloudflare Pages first, or the key gate simply keeps it off.
3. The launch/announcement gap is unchanged — nothing has ever been posted. `design/social/`
   (untracked) holds started launch material.

## 4. Gotchas confirmed this session

- `gh pr merge` still fails on this repo; use `gh api -X PUT .../pulls/<n>/merge`.
- Patching a release's `target_commitish` after its tag exists returns 422 — push the tag first
  and leave the field alone.
- `gh release upload` refuses to overwrite an existing asset; `gh release delete-asset` first.
