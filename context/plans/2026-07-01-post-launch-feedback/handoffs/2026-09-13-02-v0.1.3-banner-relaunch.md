# Handoff — 2026-09-13-02 · 0.1.2 verified on-device; v0.1.3 released (banner relaunch copy)

## 1. What happened after 2026-09-13-01

- **0.1.2 installed on the owner's machine** over the degraded 0.1.1 (DMG install, not brew).
  Migration 0008 re-sorted 32 days; the health pass wrote `capture_ax_ok=0`, `capture_auto_ok=1`
  in 36 s; the Day view banner named the Accessibility loss. Detection: verified.
- **Recovery needed a relaunch.** The owner toggled Accessibility off/on. The running process
  did not pick it up (titles still empty, flag still 0). After quit + reopen the pass cleared
  the flag in 33 s and Chrome titles returned at once. The banner copy hadn't said to relaunch.
- **PR #43** (merged): both permission banners end "then quit and reopen UsageOS"; test extended;
  `THIRD-PARTY-LICENSES.html` regenerated (notification plugin tree, as shipped in 0.1.2).
- **v0.1.3 released**: tag `6acd59f`, same recipe (script → DMG notarized separately → staple →
  Gatekeeper-clean). DMG sha256 `95504fcfc4cfdf0c457da6beda144a263e6171b1ff5e2ff2afd7e6f9cca57888`.
  Cask bumped. **Landing link bumped only after the release was public** — the reverse order
  is what produced this morning's six-week 404.

## 2. Open

- [ ] Health pass should notice a live TCC change without a relaunch (plan item 4 follow-up).
- [ ] PR #42 (landing PostHog) — still open, still needs the key in Cloudflare Pages.
- [ ] The owner's own machine is on 0.1.2; the updater should offer 0.1.3 (first real test of
      the opt-in updater across a version bump, and of D69's SMAppService-survives-update claim
      if start-at-login is on).
- [ ] Nothing announced yet.
