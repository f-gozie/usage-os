# Handoff — 2026-07-02-02 · PRs #37 + #38 merged; v0.1.1 re-cut in place

## 1. Current state
- **PR #37 (About version) and PR #38 (SMAppService login item) are merged** (one plan.md
  conflict between them, resolved by keeping both items in order). Branches deleted.
- **v0.1.1 was re-cut in place** (owner call: replace the release rather than cut v0.1.2, keeping
  two releases total): tag `v0.1.1` force-moved to `c0aeb49` (merged main + changelog line), all
  three assets rebuilt, notarized, DMG manually stapled (same tauri gap as before), uploaded with
  `--clobber` under the **same asset names** — so the landing URL and cask URL kept working with
  zero edits. Cask sha256 bumped (`f-gozie/homebrew-tap` `8c409cc`). Updater endpoint verified
  serving the new manifest.
- New DMG sha256: `047bfa4e3bab87f03640f0f9b4eaf9d34e29df612727f4c6c60cd16b291d2600`.

## 2. Consequence of same-version re-cut (accepted)
Installs from the *first* 0.1.1 build (the owner's, possibly the tester's) **won't self-update**
— the updater sees 0.1.1 == 0.1.1. Fix: reinstall from the DMG (or `brew reinstall --cask
f-gozie/tap/usageos`). Data is untouched (same store, same bundle id).

## 3. Owner next steps
1. **Reinstall from the new DMG** (download from the release page or the landing button) —
   quit the running copy first (tray → Quit).
2. Flip **Start at login** on → System Settings → Login Items should show **UsageOS with its
   icon**; the background-items notification (if it re-fires) names UsageOS.
3. **Log out / log in** — the app should appear only in the menu bar (no window, no Dock icon);
   toggling off later must NOT kill the app (trampoline, verified pre-merge but a real login is
   the true test).
4. Reply to the tester — everything they asked for is now in the shipped 0.1.1.

## 4. Watch items
- D69's recorded expectation: the SMAppService registration survives in-place app updates —
  first real evidence comes with the next version bump (0.1.2).
- The re-cut left the release NOTES untouched (still accurate); only assets/tag changed.
