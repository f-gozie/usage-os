# Usage OS — Product Redesign: Dial + Recap + Local AI

**Task:** TBD (assign in workflow) · **Created:** 2026-06-22 · **Updated:** 2026-06-24 · **Status:** 🟡 In progress — **Phases 0–1 ✅; Phase 2 substantially done.** Day dial ✅, Week ✅, Timeline + D34a ✅, **M4 Settings ✅** (D42) and **M4.1 ✅** — app icons app-wide, the categories/rules legibility overhaul (picker, conflict warning, Uncategorized section, copy), Timeline latest-first, and the **full Context→Category rename** (D43–D46) — all on `phase2/m4-settings` (**uncommitted**). **Phase 2 now complete** — categorization closed out via the **D47 pivot** (embeddings spiked + shelved as below-baseline; instead app-metadata suggestions + relatable defaults **Work·Browsing·Messaging·Entertainment·Personal**). **Phase 3 recap ✅ DONE** — the Foundation Models sidecar (D51) + persisted content-fingerprint cache (D52), merged via [#17](https://github.com/f-gozie/usage-os/pull/17) (squash `c58767e`) after `/usageos-review` (report `reviews/2026-06-24-phase3-recap-sidecar.md`; 0 critical, W1 useRecap race + W2 sidecar-kill-on-timeout fixed pre-merge). Only the deferred evening "your day is ready" ping remains. **Phase 4 shell & onboarding ✅ DONE** — menubar tray + glance NSPanel, first-run onboarding, permission seam, strict CSP (D55/D56/D57); verified on-device + `/usageos-review`'d; PR [#19](https://github.com/f-gozie/usage-os/pull/19). **Phase 6 underway (sequenced BEFORE Phase 5 launch — owner's call 2026-06-25)** — optimization/performance/stability/security, **led by stress & performance testing**. **Session 1 ✅ (`phase6/perf-harness`): scale + read-path baseline** — built a reusable `perf`-feature harness (synthetic-history generator + read-path timing test + `seed_db` bin); **headline: the read path is O(total history) and the unbounded overlap query is the sole bottleneck — the rollup build is flat ~1.2 ms; `get_week` breaches first at ~15 months.** Fix deferred (measure-first); see `explorations/2026-06-25-perf-baseline.md`. The **unreproduced 2.13 GB memory** question still lives here (release-build long-run + heap snapshot before any fix — the baseline shows the Rust side is tiny, so look webview-side). **Phase 5 (launch)** follows Phase 6. Tracking + Local-intelligence settings groups still deferred (native / Phase 3); day-start offset (D14) is the last deferred Phase-4 item.

> Builds **on** the current `main` (v0.1.0 + the Tier-1 OSS-hygiene foundation). This is an evolution, not a restart. See `context/vision.md` (what/why) and `context/decisions.md` (D1–D25).

## Goal

Turn the existing tracker into a private, on-device **time mirror**: a fixed-24h **day-dial**, a daily **recap**, two-axis context × project, optional local AI — in a Bauhaus design. Cut gamification.

## What already exists (keep & build on)

From the shipped v0.1.0 + Tier-1 work, do **not** rebuild these:
- Tauri v2 shell, **rusqlite** + a versioned **migration system** (`schema_migrations`), settings table.
- Category **rules engine** (process/title matching, `ignore_title`), reprocess logs.
- **Tests** (24 Rust, 28 TS) and **GitHub Actions CI** (Linux/macOS/Windows), CONTRIBUTING, CHANGELOG, data **retention**.

## What changes (the redesign)

- **Capture:** `active_win_pos_rs` (app-name only; titles empty) → event-driven **AX titles + Automation URLs + NSWorkspace** events via `objc2`. _Still broken today — this is task #1._
- **Data model:** extend the migration chain (v5+) — add `projects`, `sites`, richer `contexts`, `recaps`, `embeddings`, `exclusions`, and columns on events (`url`, `site`, `project_id`, `is_private`).
- **UI:** the cyberpunk pie dashboard → the **Bauhaus day-dial + week + recap + linear timeline** (light & dark).
- **New deps:** `tauri-specta` (generated IPC), a thin **Swift Foundation Models sidecar** for the smart recap (template recap as fallback), on-device embeddings for categorization.

## Constraints

- Capture/AX/Automation can only be validated on macOS (Favour's machine). The CI/Linux box can build + run non-GUI Rust/TS tests but not the desktop capture path.
- Hard rules in `CLAUDE.md` are binding (no network in data path; generated IPC; no `unwrap` in prod; SQL only in the repo layer; design from the system).

## Phase 0 — De-risk & foundation

- [x] **Ecosystem research + whole-project feasibility audit** — wrote `context/standards/{rust,tauri-ipc,capture-and-permissions,testing-and-ci,foundation-models}.md` + `context/feasibility/2026-06-22-feasibility-audit.md` (verdict **GO-WITH-CAVEATS**; risk register R1–R83; spike plan §4). Desk-grounded from current sources; **native/version claims provisional until the spike confirms them.**
- [~] **Native capture spike (isolated)** — on the real Mac. _Make-or-break._ **Spike order (audit §4):**
  - [x] ① **AX titles with Screen Recording OFF (R2–R4, R7) — ✅ PASS** ([PR #6](https://github.com/f-gozie/usage-os/pull/6), `spikes/ax-titles/`). Real focused-window titles for Chromium (Chrome, Brave), Electron editors/apps (Cursor, Claude, Notion, Figma, WhatsApp, Spotify), native (Finder), terminals (iTerm2) — Accessibility only, no Screen Recording. **R4 retired.** Side-findings: a run loop is required (confirms R6); system-wide `AXFocusedApplication` errors from a CLI (use per-pid); titles already carry project/page signal (helps D6) **and** sensitive content (D8 is load-bearing).
  - [x] ② **run-loop/threading model + NSWorkspace activation + AXObserver — ✅ PASS** (`spikes/ax-observer/`). The real event-driven architecture works end-to-end, Accessibility-only: the `NSWorkspace` activation **block** (`block2::RcBlock`) fires on every switch (R8/R9), a **per-PID `AXObserver`** delivers `AXFocusedWindowChanged` through the run-loop source and is rebuilt per app (R10), a dedupe debounce coalesces chatty titles (R11, observed `coalesced 3`), and every event marshals to a **Tokio** consumer over a `Send` channel with idle **0.0% CPU** (R6). Built + ran on **arm64** (R13). **Key findings:** (a) **`AXObserver` lives in `objc2-application-services`** — `accessibility-sys` not needed; (b) **no `NSApplication` required** — a bare main run loop delivers activation events (Tauri's loop is a superset); (c) **`AXTitleChanged` must be registered on the focused *window*** and re-pointed on window change. Residual manual check: pure in-place `AXTitleChanged` delivery (browser-navigation pass). R12 (idle detection) is out of scope (already shipped, orthogonal).
  - [x] ③ **browser URL + incognito exclusion — ✅ core PASS** (`spikes/browser-url/`). Chromium active-tab **URL reads** via Apple Events (Chrome, Brave; R15), and an **incognito window is excluded** because `mode of front window` is read **first** and any non-`"normal"` mode skips the URL — proven **live** (normal→incognito→normal; R17, D8 enforced). osascript latency **~140–160 ms** warm, fine for event-driven (R21); `NSAppleScript` not needed. Automation grants are per-(client,target) and persist (R19); the TCC client is the responsible process (the `.app` bundle in production, R20). **Residual manual checks:** Safari URL + private-detection (R16/R18 — Safari wasn't running; safe-default = never emit a Safari URL we can't prove non-private), Arc (R17 window/space model), and the `-1743` deny fallback (coded, not live-triggered). Zero-dep pure-std crate shelling `/usr/bin/osascript` (C7).
  - [x] ④ **`proc_pidinfo` cwd read — ✅ PASS** (`spikes/proc-cwd/`). A **non-root, unsandboxed** process reads another process's cwd via `proc_pidinfo(PROC_PIDVNODEPATHINFO)` — **no `EPERM`** (R22 was the kill-switch). Verified against interactive zsh shells it didn't spawn, matching `lsof` exactly (a shell in `…/projects/usage_os` → that path; `…/projects/nudge` → that). Hardened-runtime GUI apps (Cursor, Finder) read fine too — same-uid is the only gate; **no TCC grant needed**. So the terminal-cwd branch is viable and **R24's iTerm2-`path`/Terminal-tty fallbacks aren't needed for the common case**. Remaining = pid *selection* (front-tab shell pid via `proc_listchildpids` + tty/recency, or iTerm2 `path`) — mechanism, not feasibility. `libc`-only crate.
  - _Note: dev-build trust held across rebuilds via `codesign --force --sign -` (R14); for the spike, the binary read cross-app titles fine once trusted._
- [x] **Project-inference spike — ✅ PASS (snapshot)** (`spikes/project-inference/`). On a corpus of real signals (live-resolved terminal cwds + real browser tabs + editor titles), the heuristic made **zero false assignments** — identified the 3 active projects (`eyemark_frontend`, `usage_os`, `nudge`) and abstained on everything else (22 signals → 8 assigned, 14 abstained). **`cwd → git remote` is the anchor** (canonical `owner/repo`, no FPs); `github-url` corroborates (R26). **Abstain threshold set** (R27): assign only on HIGH/MED unambiguous signals, else `unassigned` — and **`ambiguous` (localhost/dev-dashboards) is a distinct third state** for Phase-2 temporal correlation. **Headline finding: project identity needs canonicalization** — same project arrived as `f-gozie/usage-os` *and* `usage_os`; the `projects` table must key on the git remote with folder/title/URL as aliases (→ D30). _Snapshot measures precision + abstain, not multi-day recall — re-measure once Phase-1 persists data._
- [x] **Hard-rule-3 cleanup + missing merge gates** ([PR #5](https://github.com/f-gozie/usage-os/pull/5)) — removed all production `.expect()` (`lib.rs`, `watcher.rs`, `db.rs` → shared `now_unix()` helper), added `#![cfg_attr(not(test), deny(clippy::unwrap_used, expect_used, panic))]` to both crate roots, and wired `clippy -D warnings` + `cargo fmt --check` + `tsc --noEmit` into `ci.yml`. (R82) _Binding-freshness gate deferred until tauri-specta is wired._
- [x] **`recharts` removed** — the Bauhaus dial replaced the cyberpunk dashboard in the Day-view cutover (`phase1/bauhaus-day`), so `recharts` (+ `lucide-react`, `@radix-ui/react-tabs`, `class-variance-authority`) were uninstalled and `ActivityChart.tsx` deleted.
- [x] **Wire tauri-specta — ✅ DONE** (`phase1/tauri-specta-ipc`). All 11 commands `#[specta::specta]` with a typed `AppError`; `get_watcher_status` (`serde_json::Value`→`WatcherStatus`) + `get_settings` (tuples→`Setting`) rewritten to named structs; `tauri_specta::Builder` replaces `generate_handler!`; `BigIntExportBehavior::Number` (timestamps export as `number`); `export_bindings` `#[test]` → generated `src/bindings.ts` (deterministic); frontend `src/lib/tauri.ts` re-exports generated types + unwraps `Result`; Linux-only binding-freshness CI gate. **Key finding (D27): the trio is `rc.20`, not the standard's provisional `rc.24`** — rc.24's specta dropped `#[specta(rename)]` on containers, which tauri 2.9.3 still uses, so rc.24 doesn't build. Commands-only stands (#211). Gates green: build, 26 Rust tests, clippy -D, fmt, tsc, 28 TS tests.
- [x] **Design system (parallel track) — ✅ FROZEN + ported.** The full Bauhaus system shipped as the frozen `design/` HTML library + `context/design-system.md` (design session), then ported into React components in the Day-view rewrite (`phase1/bauhaus-day`): tokens → `src/styles/tokens.css`, Tailwind rewired to the tokens, fonts (Anton/Jost) bundled locally via `@fontsource` (no CDN — hard rule 1), `ThemeProvider` (paper/warm/black via `[data-theme]`, persisted in settings), and a Storybook (10, react-vite) with a 3-theme toolbar. DesignSync push still deferred (needs an interactive `/login`; run after this lands). _Original in-progress note:_ **In progress (design session 2026-06-23):** built as interactive, self-contained **HTML mockups under `design/`** — Claude Design `/design-login` is unavailable in this CLI env, so the DesignSync push is deferred and the mockups are the working visual source of truth (to be pushed/reconciled later). **Locks so far:** **R77 resolved → Option A** — every dial arc carries a 1px ink **casing** (clears WCAG 1.4.11 non-text contrast regardless of fill) + Comms tuned to gold `#EAB308` + colour is **never the only cue** (legend/ledger/inspector always carry the text label); contrast re-run. Token themes: `paper` + two darks (`warm` charcoal / `black` near-black — **canonical dark TBD**, both shippable as user-selectable for ~zero cost). Theme-safe inverted bars via `--bar-bg`/`--bar-fg`; the week "now" marker is the same theme-aware triangle as the day dial. **Screens mocked:** Day, Week, Settings. **Pending:** Timeline, a Components & States page (inputs/dropdowns/time-picker/forms/**modals**), onboarding + permission-priming, landing; then freeze `context/design-system.md`.

## Phase 1 — Capture → the dial ("I open it and see MY actual day")

- [x] Replace the watcher with the event-driven capture impl (behind a `capture` trait, mockable). **1.2a ✅** (`phase1/capture-seam`, D32): the seam — trait + `FocusEvent` + `process_focus_event` + `FakeCapture` + interim `PollingCapture`; `watcher.rs` deleted. **1.2b ✅ code-complete** (`phase1/capture-macos`, D33): event-driven `MacosCapture` (NSWorkspace + per-PID `AXObserver` on the main run loop), browser URL + incognito skip, terminal cwd + pid selection, inline `enrich/` (site + project inference, D30); channel → `std::mpsc` + dedicated consumer thread; `default_source()` flipped on macOS. Compiles + clippy on macOS CI; **runtime pending the on-device checklist** (Accessibility/Automation grants). Deferred: heartbeat timer (C2) + full permission priming (Phase 4/D21). No IPC change.
- [x] **New migrations for projects/sites/contexts + event columns; repository functions + tests — ✅ DONE** (`phase1/data-model`, D31). Append-only migrations v5–v8: `projects`+`project_aliases` (D30 canonicalization), `sites` registry, `exclusions` (D8), and `url`/`site`/`project_id`/`project_abstain_reason`/`is_private` columns on `activity_logs`. Typed repository (`resolve_or_create_project`, `match_exclusion`, `insert_event`/`NewEvent`, …) + 14 new tests (40 Rust total). WAL on; `db` is now `pub mod`. Renames `activity_logs`→`events` / `categories`→`contexts` deferred to the UI rewrite (D31). Only `ActivityLog` touched `bindings.ts` (additive).
- [x] Sensitive handling: exclusion list, per-app Private, incognito never recorded. _(Schema + `match_exclusion` repository fn landed in 1.1; capture-time wiring is in `MacosCapture` (incognito titles/url dropped, D33); the **Settings UI** to add/remove exclusions (App/Site/Title × Exclude/Private) + the always-on incognito row landed in **M4** (D42, `phase2/m4-settings`).)_
- [x] **Fixed-24h dial from real data, click-to-inspect — ✅ DONE** (`phase1/bauhaus-day`; D35, D36; impl-plan `2026-06-23-bauhaus-ui-day`). Backend-first: hardened the migration runner + squashed to a clean baseline with a contexts `slug` (D35); added the pure **`rollup`** read-model + **`get_day`** command (per-axis aggregates + context-runs + deterministic template recap — D34/D36, numbers in Rust); seeded the 4 canonical contexts + **starter rules** (migration 3) so the dial is meaningful out-of-the-box. Frontend: the React `Dial` (SVG, ported from the mockup) + `DayView` (recap, dial, stats, legend-isolate, inspector, ledger; loading/empty/degraded states, no mock data) inside the new shell. Cutover removed the cyberpunk dashboard. Gates green: 69 Rust + 7 TS tests, clippy/fmt, tsc, vite + storybook build, bindings fresh. **Day-navigation works (prev/next); the dial centre/ledger/recap reflect the viewed day.** **✅ Run on-device 2026-06-23:** capture worked but exposed a zero-duration-span bug (dial showed 0) + a window-in-a-window shell — fixed in **D37** (span durations + Overlay titlebar/sizing/tracking/theme-switcher), then the capture write-path was **rebuilt as a single-writer state machine** in **D38** (decided via a Codex↔Opus debate → the new `/debate` skill): one writer owns the open span, self-ticks via `recv_timeout` on a wall-clock idle deadline, resumes the same window after idle, 2 knobs (TICK=20/GATE=120, dogfood-tunable). Idle is now an untracked gap (no idle rows). **✅ D39 (2026-06-23):** agent-supervision tracking (a second Codex↔Opus debate, 2 rounds → Position A) — a **per-app idle gate** (default 120s; **600s, capped** for agent/dev surfaces in `PATIENT_APPS`) so hands-off agent-watching counts as Deep work instead of being dropped; **no** agent-detection and **no** separate "attended" dimension; the consumer is now the **single idle source** (vestigial producer-side `is_idle`/180s threshold removed); Claude desktop app Research→Deep (migration `0004`, rule + backfill); the Day headline tile shows the **leading context** (was a hard-coded "Deep work" that read as a broken "14s"). 73 Rust + 7 TS tests green. `PATIENT_GATE_SECS=600` stays D34a dogfood-tunable. Next: dogfood real days to tune GATE/PATIENT_GATE + starter rules (D34a)._

## Phase 2 — Enrichment

- [x] **Categorization — ✅ DONE (closed out via the D47 pivot).** Contexts/rules editor shipped in M4 (D42). **Embedding-based categorization was spiked then SHELVED** — `spikes/embeddings/` measured `NLEmbedding` similarity **below the majority-class baseline** (app-name LOO 39% / centroid 41% / title 60% vs **43%** "always guess the biggest"; brand names are out-of-vocabulary noise), so it was **not built**. Instead (D47): rules stay the deterministic categorizer + **D44's Uncategorized→Assign flow IS the corrections memory** (a correction becomes a retroactive `process` rule); added **`LSApplicationCategoryType`→category suggestions** — a conservative/abstaining `apps::suggest_slug` (via `NSBundle`) surfaced as a **one-click "sort into X"** in the Uncategorized list (suggestion only, never auto-assigns); and a **relatable default taxonomy** — **Work · Browsing · Messaging · Entertainment · Personal** (5th canonical, theme-aware `--c-personal` green; migration `0006`, fresh-installs-only so existing DBs are untouched). _The mechanism (embeddings callable in Rust, off-thread, 512-d, no Apple Intelligence) works — only the signal is wrong; the spike crate is kept as the record. 99 Rust + 20 TS tests, clippy/fmt/tsc, vite + storybook green._
- [x] **M4.1 — App icons + categories/rules legibility + Timeline latest-first + Context→Category rename — ✅ DONE** (`phase2/m4-settings`; impl-plan `2026-06-24-m4.1-app-icons-contexts`; **D43–D46**). From dogfooding M4: (a) an offline installed-app **catalog** (`apps` module + `list_installed_apps` — `sips`/`.icns`, disk-cached, scans Chrome/Brave PWA dirs; 118 apps/114 icons) → a shared **`AppIcon`** primitive (resolve-by-`process_name` + monogram fallback, map warmed at startup) wired into the **Timeline** switch rows + run line and **Settings** (the category-row chips); (b) the **category editor rebuilt** — app-picker (no blind text), first-match-wins **conflict warning** + "Move it", live "sorts N apps" count, **Advanced→title** drawer with corrected copy (substring, not wildcard); (c) a new **Uncategorized** Settings group (`get_uncategorized_apps` — all-time, ranked, floored 60s) with one-click **Assign**; (d) the **exclusion modal** gets the same app-picker; (e) the **Timeline reordered latest-first** (Now on top); (f) responsive category-row spacing (long names wrap, chips reflow); (g) **D46 — full Context→Category rename** across UI + IPC + Rust + frontend (heals the SQL/IPC/UI split; SQL was always `categories`). 97 Rust + 20 TS tests, clippy/fmt/tsc, vite + storybook green. _The 2.13 GB dev-build OOM is **UNREPRODUCED** — a single Force-Quit reading under system-wide memory pressure; monitoring showed Rust flat ~100 MB and WebKit fluctuating 58–290 MB (spike recovered, no sustained climb), so "Timeline DOM / virtualize" is an unproven hypothesis, not a diagnosis. Moved to **Phase 6** to diagnose properly (release-build long-run + heap snapshot) before any fix._
- [x] **Week view (7 mini-dials) — ✅ DONE** (`phase1/bauhaus-day`; impl-plan `2026-06-23-week-view`): pure `rollup::build_day_slice`/`build_week_view` + the `get_week` command (per-day active/deep + dial runs, week totals — numbers in Rust, hard rule 6); React `WeekView` (summary tiles active / avg-per-day / deepest-day, week nav, click-a-day → Day view) + `MiniDial` (shared dial geometry). 76 Rust + 12 TS tests, vite + storybook builds green. **Timeline (agenda of context-runs + click-to-expand app-switch detail + Away/Now rows) — ✅ DONE** (M3; `phase1/bauhaus-day`; impl-plan `2026-06-24-timeline-view`): pure `rollup::build_timeline` (`TimelineRun` w/ inner `TimelineSegment`s) + `get_timeline` command; React `TimelineView` + reusable `TimelineRow`. 79 Rust + 14 TS tests, builds green. **Timeline = context-runs with project shown inside (D34, Direction A);** dial arcs are context-runs too. A read-time rollup over `events` (no schema change). **✅ D34a / excursion-absorb — DONE (D40):** the Timeline surfaced over-fragmentation → resolved via a second two-round Codex↔Opus debate. Segmentation **unified** into one `build_segmented_runs` pass (raw runs → fixpoint absorb → rich `TimelineRun`s; `ContextRun` is the projection — dial/timeline/week can't diverge) with **excursion-absorb**: a sandwiched non-X cluster ≤ `ABSORB_SECS` (90s wall-clock) folds into the host run, guarded by **local dominance** (host active ≥ excursion) + an **accumulation cap** (`MAX_ABSORB_FRACTION_PCT` 15%); run headline numbers stay **host-context-only**; the detour lives as a segment (marked with its context) + the per-axis ledger. 86 Rust + 14 TS tests, builds green. `ABSORB_SECS`/`MAX_ABSORB_FRACTION_PCT` are **dogfood-tunable**. **✅ D41 (2026-06-24, first dogfood tune):** reading a real day surfaced (a) a capture bug — `loginwindow` lock time counted as active (46 min; `user_idle` reads ~0 while locked so the gate never fired) → fixed by treating `loginwindow`/`ScreenSaverEngine` as **away** (close span, drop, don't resume), and (b) real switches cluster at 90–300s → **`ABSORB_SECS` raised 90→180s**. See `explorations/2026-06-23-sessionization.md` + D40/D41.

## Phase 3 — The recap

- [x] **`RecapFacts` computed in Rust + a richer deterministic template recap — ✅ DONE (D48).** `compute_recap_facts` (in `rollup`) pulls the day's facts — leading + runner-up category, leading project (≥40%), longest continuous stretch (≥15 min) with a local time-of-day phrase (`(run.start − day_start)/3600`, no TZ lib) — and `render_template_recap` phrases them **purely factually, never evaluative** (calm mirror, not a coach). `build_day_view` now takes `day_start` (the `get_day` start). 101 Rust tests; no IPC/frontend change (the Day view already renders `recap.text`). _The Foundation Models sidecar (next) reuses `RecapFacts`; this stays the always-on fallback._
- [x] Swift `usageos-ai` sidecar (Foundation Models, stdio, structured output) + availability check + fallback. **✅ DONE (D49 spike + seam; D51 build).** **Spiked → VIABLE (D49)** (`spikes/foundation-models/`: compiles vs the real SDK, `available` on this Mac, ~1–2s warm, quality risks + fixes found). **Chunk A** — the Rust `ai::Narrator` seam + `FakeNarrator` + `build_recap` (fallback to the D48 template) + `format_recap_prompt`. **Chunk B** — productionized Swift sidecar (`sidecar/usageos-ai/`): unbuffered `FileHandle` stdout (pipe gotcha), JSON `{prompt}`→`{status,text,ms}`, `--serve`/`--prewarm`, empty entitlements (no network). **Chunk C** — `tauri-plugin-shell` (pinned `=2.2.1` to hold the proven tauri 2.9.3 stack), `externalBin`, scoped capability; `SidecarNarrator` (one-shot spawn, line-buffered, 20s timeout, status-tag branch); off-thread `prewarm()` at launch. **Chunk D** — async `get_recap` (lazy, after the dial — D11) via new `rollup::build_recap_facts`; `useRecap` upgrades the recap card template→AI with the "⌁ Summarized on-device" badge (`generated_by === "foundation-models"`). Impl-plan `2026-06-24-phase3-recap-sidecar`. 111 Rust + 23 TS tests; sidecar verified on-device. _CI: stub sidecar for cross-platform build (externalBin is compile-time-validated) + non-blocking macOS-26 Swift lane (C20)._
- [~] Lazy compute on open; opt-in evening "your day is ready" ping. **Lazy/async `get_recap` ✅ DONE** (chunk D / D51 — computed off the day-load path, card upgrades in place). _The evening ping stays deferred._

## Phase 4 — Shell & polish

- [x] **Menubar launcher + window; primed onboarding + permission priming — DONE, verified on-device + `/usageos-review`'d (D55/D56/D57); pushed, PR pending.** Branch `phase4/shell-onboarding` (on `origin`, commit `c6507fd`); impl-plan `2026-06-25-phase4-shell-onboarding`; review `reviews/2026-06-25-phase4-shell-onboarding.md`. Menubar **tray + hide-on-close** (closing hides; tracking keeps running; quit only via tray); left-click opens a **share-donut glance popover** (`#/glance`, a non-activating NSPanel — D56); first-run **onboarding** (Welcome → Privacy → Accessibility → Automation → Ready, skippable to degraded) gated on an `onboarding_completed` setting, re-grantable from **Settings → Permissions**; a `permissions` seam (AX trust + `AEDeterminePermissionToAutomateTarget`, no-prompt status). **Verified on-device:** glance floats over full-screen + themed + positioned; onboarding Scenario A (skip→degraded) + B (grant→redirect→pill flip); tray Quit exits; hide-on-close keeps tracking; CSP renders. Review fix **D57** — capture no longer force-prompts (promptless skip); + tray-failure fallback, glance dismiss debounce, shared `useWindowFocus`/`GrantedPill`. 125 Rust + 32 TS green. _Native tray/AX/AppleEvent paths verified on Favour's Mac, not CI (D32/D33)._
- [~] Dark-mode parity (token-only components — onboarding + glance support all 3 themes); idle CPU (glance/permissions poll on focus, not a timer). Glance + onboarding **rendered + themed correctly on-device** (warm theme); full 3-theme sweep + idle-CPU profiling still light. No separate code beyond D55.
- [x] **Settings — Your-data controls — ✅ DONE in M4** (D42, `phase2/m4-settings`): retention duration picker (`set_retention_days` wrapping `cleanup_old_data` / `data_retention_days`); **Export CSV** (`export_events_csv` → writes next to the DB, reveals via `opener`); **Delete all data** (`delete_all_data` — wipe record, preserve config — behind a type-`DELETE` confirm modal); **Show in Finder** (`get_database_path` + reveal). _Pulled forward from Phase 4 since M4 was building the Settings shell anyway; the data-ownership/privacy ethos (`vision.md`) justifies them._
- [ ] **Day-start offset** (night-owl "day starts at 4 AM", D14): applied to day-bucketing + exposed in Settings. _Deferred "later" per D14; the design tags it as such — build after the core dial lands._

## Phase 5 — Launch _(deferred until after Phase 6 — owner's call 2026-06-25: harden + perf-test before shipping)_

- [ ] Notarized DMG + auto-update + Homebrew cask; rewrite README for the new product; Sponsor link; finalize name/domain.

## Phase 6 — Optimization, performance, security & hardening (the thorough final pass) — **NOW NEXT (before Phase 5)**

_The dedicated quality phase before the public 1.0 (and ongoing): full code reviews, performance + memory profiling, security review, and hardening. Be exhaustive here — this is where deferred quality work and dogfood-surfaced concerns land._ **Owner sequenced this before Phase 5 launch (2026-06-25): we stress + performance test the app before shipping it. Stress & performance testing is the lead workstream.**

- [~] **Stress & performance test the app — THE NEXT FOCUS** (owner directive 2026-06-25). Build the load/soak harness and find where it bends before real users do: a **large synthetic history** (seed many months / millions of `activity_logs`) to test rollup/`get_day`/`get_week`/Timeline cost at scale; a **long soak run** (release build + RSS/CPU monitor, ties into the 2.13 GB item below); **high-churn capture** (rapid app/window switching, idle/resume, multi-display) to stress the single-writer consumer + the `Arc<Mutex<Connection>>` path (R57); **recap/sidecar** under repeated spawns. Output: numbers + the bottleneck list that drives the rest of this phase.
  - **DONE this session (2026-06-25, `phase6/perf-harness`) — measured, fixed, re-measured, hardened.** Full results: `explorations/2026-06-25-perf-baseline.md`.
    - **Harness** (behind a `perf` cargo feature, never ships): deterministic synthetic-history generator (`src-tauri/src/perf.rs` → `db::bulk_insert_events`, repo-layer), `#[ignore]` read-path timing + write-churn tests, a `seed_db` bin for an on-disk DB.
    - **Read path — diagnosed + FIXED.** Baseline showed it was O(total history) (the rollup build is flat ~1.2 ms — never the bottleneck): `get_activity_logs`'s `start_time < ?end` (only `idx_start_time`) scanned ~the whole table for a recent day. **Fix:** lower-bound the scan at `start − MAX_SPAN_LOOKBACK_SECS` (2 days) so the index does a bounded range scan — one change in `db/events.rs`, no migration, planner-independent (regression test added). **Before→after @ 1.05M rows: `get_day` 52.97→1.59 ms (~33×), `get_week` 370.9→10.5 ms (~35×).** Now **flat across all scales** — every read well under the proposed budgets at 1M+ rows, so the "single Week query"/"shared fetch" ideas are unnecessary.
    - **Write path — churn + lock contention measured → R57 deferred with data.** 45k writes/sec solo; under a tight-loop concurrent reader the writer slows ~16× (single `Arc<Mutex<Connection>>` serializes) but real rates never approach that. R57 (dedicated writer thread) **correctly deferred** — the churn gate says no real contention.
    - **Memory** — live dev app RSS ~170 MB (main 90 + WebKit 82); combined with the tiny Rust read footprint, **strong evidence the 2.13 GB was transient/system-pressure, not a product leak** (the disk filled during this session's builds — a plausible source of that pressure).
    - **Security** — `cargo deny` is now a committed gate (`src-tauri/deny.toml`): **2 real vulns fixed** (`bytes`→1.11.1, `time`→0.3.47), **our crate declared `license = "MIT"`** (was undeclared), licenses/sources clean, unmaintained-transitives documented. No-network reaffirmed (reqwest not in build graph).
  - **Remaining Phase-6 (later sessions):** the optional confirmatory release soak (heavy DB + long Timeline nav — `seed_db --months 24`); native `osascript` off the main run loop; migration `CHECK` constraints; idle-CPU profiling.
- [~] **Diagnose the 2.13 GB memory observation — strong evidence it's NOT a product leak** (release soak optional/confirmatory). One Force-Quit reading of 2.13 GB during a long dev session under *system-wide* memory pressure; ~28-min monitoring showed **Rust flat ~100 MB** and **WebKit fluctuating 58–290 MB** (the spike recovered — no sustained climb). **Not diagnosed.** Path: (1) run a **release** build (`tauri build`) for a long session with the RSS monitor — flat ⇒ dev-mode bloat (HMR/devtools/debug binary), no product bug; climbs ⇒ real. (2) If real, take a **WebKit heap snapshot** to name what's retained (DOM? our data? something else). (3) Only then choose a fix — *maybe* Timeline list virtualization, maybe not. Do not build a "fix" before reproducing. _(Phase-6 evidence: the read path holds only ~1,030 events/day in memory regardless of history; the **live dev app sampled at ~170 MB** (main 90 + WebKit 82), consistent with the earlier healthy range. And this session's repeated builds filled the disk (`ENOSPC`) — a plausible source of the original "system-wide pressure." Leading conclusion: transient/dev-mode, not a leak. A release soak (heavy DB + long Timeline nav) would confirm definitively.)_
- [ ] **Performance pass:** idle CPU (pulled from Phase 4), capture overhead, rollup cost on large multi-month histories, webview render cost on very long Timelines/days.
- [x] **Security review** + dependency audit; reaffirm *no-network-in-the-data-path* (hard rule 1) + the exclusion/private (D8) paths. **Done as part of the full-codebase audit (D53):** no-network reaffirmed clean end-to-end; D8 paths hardened (empty-pattern blackout guard). **Dependency audit ✅ (2026-06-25):** `cargo deny` is a committed gate (`src-tauri/deny.toml`) — **2 real vulnerabilities fixed** (`bytes` 1.11.0→1.11.1, `time` 0.3.44→0.3.47), our crate now declares `license = "MIT"`, all licenses MIT-compatible + allow-listed, sources clean, unmaintained-transitives (rust-unic/fxhash/mach/paste, no upstream fix) documented-ignored; no-network reaffirmed (`reqwest`/`hyper` resolved but **not in the build graph**). _CSP **✅ set in Phase 4 (D55)** — `tauri.conf.json` `csp` is now a strict `default-src 'self'; … object-src 'none'; frame-src 'none'` (no remote anything); on `phase4/shell-onboarding`, verify-no-white-screen is on its on-device checklist._
- [x] **Code-review sweep** across the redesign surface — ✅ the **first whole-codebase `/usageos-review` audit (D53)**: 14-subsystem Claude panel + 7 Codex lanes, every finding verified. 0 critical / no hard-rule violations. Report + raw findings in `reviews/2026-06-25-full-codebase-audit.md`; resolved on `phase6/audit-fixes` (impl-plan `2026-06-25-audit-fixes.md`) — correctness bugs (slugless categories, capture idle-inflation, day-boundary clipping, reprocess parity, enrich hardening) + a simplification sweep (net −184 LOC). 114 Rust + 27 TS green.
- [~] **Hardening:** ✅ migration drift-on-missing detection + icon-cache key collisions fixed (D53); production error paths reviewed. **R57 (WAL dedicated writer thread) — measured + still deferred:** the write-churn test shows writes at 45k/sec and contention only under tight-loop concurrency that real interaction rates never hit, so the single `Arc<Mutex<Connection>>` is fine and R57 stays perf-gated (the churn harness is the trigger). **Still deferred:** native capture `osascript`-off-main-loop + producer full-identity dedupe (on-device session); migration `CHECK` constraints (needs a table-rebuild migration).
- [ ] **Test coverage** measured (D53): Rust core ~90%+ (`rollup` 99 / `migrations` 99 / `db` 94 / `enrich` 93), native FFI 0% (verified on-device by design), frontend logic strong / interactive components lighter. Standard: keep the deterministic core well-tested; **no flat repo-wide % gate** (would punish on-device-verified native code). Add `CategoryEditorModal` component tests next.

## Open questions

- Project-inference accuracy (Phase 0 spike) may reshape the project axis.
- Foundation Models recap quality on a ~3B model — validate grounded prompts.
- How much of the existing cyberpunk UI / category model carries over vs is replaced.
